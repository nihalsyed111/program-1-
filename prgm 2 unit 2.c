#include<stdio.h>
int main()
{
int i,key;
printf("enter the element to be searched:");
scanf("%d",&key);
int arr[]={5,10,15,20,25,30};
for(i=0;i<6;i++)
{
if(arr[i]==key)
printf("the element %d is found and its position is:%d\n",&key,i+1);
break;
else if
printf("elemnet is not found");
}
}

